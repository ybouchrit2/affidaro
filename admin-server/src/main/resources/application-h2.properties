server.port=8090
spring.jackson.serialization.write-dates-as-timestamps=false

# H2 in-memory database for local/dev profile
spring.datasource.url=jdbc:h2:mem:aversa_admin;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_UPPER=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=Yassin1997$$$
spring.datasource.password=MaryamYassin20021997

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Admin login credentials for h2 profile
admin.username=Yassin1997
admin.password=MaryamYassin20021997

# Connection Pooling (HikariCP) for H2
spring.datasource.hikari.pool-name=AversaHikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=1800000

# Secure session cookies (H2/dev)
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=strict

# JWT secret for H2/dev profile
jwt.secret=affidaro-h2-dev-secret-change-me-2025
# Static resources caching and compression (H2 dev)
spring.web.resources.cache.period=7d
server.compression.enabled=true
server.compression.mime-types=application/json,application/javascript,text/css,text/html,image/svg+xml,image/png,image/jpeg,image/webp,image/avif
server.compression.min-response-size=1024